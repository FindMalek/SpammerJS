export type SpamType = {
    serviceId: string;
    templateId: string;
    userId: string;
    website: string;
};

export type FormType = {
    user_email: string;
    user_subject: string;
    message: string;
};